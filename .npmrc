# node-gyp and node-pre-gyp locate each other by walking a flat node_modules
# tree. pnpm's default isolated layout hides node-gyp from node-pre-gyp, which
# then silently falls back to whatever node-gyp npm happens to bundle.
node-linker=hoisted
